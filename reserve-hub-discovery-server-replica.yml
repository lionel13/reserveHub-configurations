eureka:
  instance:
    hostname: reserve-hub-discovery-server-replica
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://reserve-hub-discovery-server:10120/eureka/,http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 10140

management:
  endpoints:
    web:
      exposure:
        include: '*'
    health:
      show:
        details: always

logging:
  level:
    root: INFO
    org.springframework: INFO
  file:
    name: logs/${spring.application.name}.log
